# Autogenerated bulkloader.yaml file.
# At a minimum address the items marked with TODO:
#  * Fill in connector and connector_options
#  * Review the property_map.
#    - Ensure the 'external_name' matches the name of your CSV column,
#      XML tag, etc.
#    - Check that __key__ property is what you want. Its value will become
#      the key name on import, and on export the value will be the Key
#      object.  If you would like automatic key generation on import and
#      omitting the key on export, you can remove the entire __key__
#      property from the property map.

# If you have module(s) with your model classes, add them here. Also
# change the kind properties to model_class.
python_preamble:
- import: base64
- import: re
- import: google.appengine.ext.bulkload.transform
- import: google.appengine.ext.bulkload.bulkloader_wizard
- import: google.appengine.ext.db
- import: google.appengine.api.datastore
- import: google.appengine.api.users

transformers:

- kind: Category
  connector: simplexml
  connector_options:
    xpath_to_nodes: /Categories/Category
    style: element_centric
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: title
      external_name: title
      # Type: Category Stats: 6 properties of this type in this kind.
      import_transform: db.Category


- kind: Flatpage
  connector: simplexml
  connector_options:
    xpath_to_nodes: /Flatpages/Flatpage
    style: element_centric
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: content
      external_name: content
      # Type: Text Stats: 5 properties of this type in this kind.
      import_transform: db.Text

    - property: title
      external_name: title
      # Type: String Stats: 5 properties of this type in this kind.

    - property: url
      external_name: url
      # Type: String Stats: 5 properties of this type in this kind.


- kind: Post
  connector: simplexml
  connector_options:
    xpath_to_nodes: /Posts/Post
    style: element_centric
  property_map:
    - property: __key__
      external_name: key
      export_transform: transform.key_id_or_name_as_string

    - property: author
      external_name: author
      # Type: User Stats: 251 properties of this type in this kind.
      import_transform: transform.none_if_empty(users.User)  # Assumes email address

    - property: body
      external_name: body
      # Type: Text Stats: 251 properties of this type in this kind.
      import_transform: db.Text

    - property: categories
      external_name: categories
      # Type: String Stats: 264 properties of this type in this kind.

    - property: created_at
      external_name: created_at
      # Type: Date/Time Stats: 251 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')

    - property: markup
      external_name: markup
      # Type: String Stats: 251 properties of this type in this kind.

    - property: published
      external_name: published
      # Type: Boolean Stats: 251 properties of this type in this kind.
      import_transform: transform.regexp_bool('true', re.IGNORECASE)

    - property: slug
      external_name: slug
      # Type: String Stats: 251 properties of this type in this kind.

    - property: title
      external_name: title
      # Type: String Stats: 251 properties of this type in this kind.

    - property: updated_at
      external_name: updated_at
      # Type: Date/Time Stats: 251 properties of this type in this kind.
      import_transform: transform.import_date_time('%Y-%m-%dT%H:%M:%S')
      export_transform: transform.export_date_time('%Y-%m-%dT%H:%M:%S')
